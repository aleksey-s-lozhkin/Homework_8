# PostgreSQL настройки
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Django секретный ключ
SECRET_KEY=your-secret-key-here

# Настройки отладки
DEBUG=False

# Язык и часовой пояс
TIME_ZONE=UTC
LANGUAGE_CODE=en-us

#Redis setting
CACHE_ENABLE=True
BACKEND=django.core.cache.backends.redis.RedisCache
LOCATION=redis://{host}:{port}/{DB}

FRONTEND_URL=http://localhost:8000
STRIPE_SECRET_KEY=your_stripe_secret_key

# ========== EMAIL SETTINGS ==========
# Для Yandex почты с SSL (порт 465)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@yandex.ru
EMAIL_HOST_PASSWORD=your-app-password-here  # Пароль приложения, не основной пароль!
DEFAULT_FROM_EMAIL=your-email@yandex.ru

# Для Gmail (порт 587 с TLS) - раскомментируйте:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Для Mail.ru - раскомментируйте:
# EMAIL_HOST=smtp.mail.ru
# EMAIL_PORT=465
# EMAIL_USE_TLS=False
# EMAIL_USE_SSL=True

CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0